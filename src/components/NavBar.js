import React from 'react';

const NavBar = React.createClass({
    render () {
        return (
            <div>
                <nav className='nav'>NavBar!!
                </nav>
                </div>
        );
    }
});

export default NavBar;