import React from 'react';

const NotFound = React.createClass({
    render () {
        return (
            <div>
                404: NOT FOUND!!
                </div>
        );
    }
});

export default NotFound;