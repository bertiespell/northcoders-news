import React from 'react';

const NotFound = React.createClass({
    render () {
        return (
            <div>
                NOT FOUND!!
                </div>
        );
    }
});

export default NotFound;