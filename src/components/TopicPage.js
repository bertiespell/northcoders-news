import React from 'react';

const TopicPage = React.createClass({
    render () {
        return (
            <div>
                TOPIC PAGE!!!! (Will render list of articles on a certain topic)
                </div>
        );
    }
});

export default TopicPage;